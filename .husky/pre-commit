# `pnpm` is only on PATH when `corepack enable` has been run with the
# privileges to write its shims. Fall back so a commit never depends on that.
if command -v pnpm >/dev/null 2>&1; then
  pnpm exec lint-staged
else
  corepack pnpm exec lint-staged
fi
